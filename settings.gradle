pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
apply from: "${rootDir}/local_dependency.gradle" //root dir
rootProject.name = "gradle-demo"
include ':app'
include ':pb'
include ':pa'
include ':pc'

